---
## Stuff to install on linux hosts
- hosts:
    - all
  become: true

  vars:
   ipmi_username: “{{ ansible_env.ANSIBLE_NET_USERNAME }}”
   ipmi_password: “{{ ansible_env.ANSIBLE_NET_PASSWORD }}”
 
  roles:
    - ipmi
